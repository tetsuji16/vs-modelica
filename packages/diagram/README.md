# @modelica-studio/diagram

Owns the renderer-independent Modelica scene graph, coordinate transforms, selection, snapping, routing, hit testing, undoable domain commands, and SVG webview rendering. Diagram and icon layers share primitives but have separate edit policies.
