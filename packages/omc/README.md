# @modelica-studio/omc

Owns OMC discovery, version negotiation, a long-lived `omc --interactive=zmq` session, typed scripting calls, timeouts, cancellation, restart, and diagnostic normalization. It must not contain copied OpenModelica implementation code.
