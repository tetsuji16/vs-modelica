# @modelica-studio/ui

Owns webview components, VS Code theme bindings, layout primitives, icons created for this project, accessibility behavior, and shared design tokens. It must not embed Modex artwork or extracted assets.
